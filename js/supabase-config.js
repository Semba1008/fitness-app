// Supabase接続情報の既定値(任意)。
// 通常はアプリの「設定」タブの「クラウド同期」カードから
// Project URL と anon public key を直接入力すれば動作します(このファイルの編集は不要)。
// ここに値を設定しておくと、その値が初期値として使われます。
// 未設定(プレースホルダーのまま)かつ設定タブでも未入力の間は、クラウド同期は無効になり、
// これまで通りこの端末内(localStorage)だけにデータが保存されます。
const SUPABASE_URL = 'YOUR_SUPABASE_PROJECT_URL';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
